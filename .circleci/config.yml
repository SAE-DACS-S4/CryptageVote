version: 2.1

jobs:
  test:
    docker:
      - image: cimg/openjdk:19.0.2
    steps:
      - checkout

      - run:
          name: Test de l'application
          command: mvn test


workflows:
  IntegrationContinue:
    jobs:
      - test
            
            
